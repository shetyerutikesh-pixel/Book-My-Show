apiVersion: v1
kind: Service
metadata:
  name: bms-service
spec:
  type: NodePort   # 🔥 Change from LoadBalancer (since you're using kind)
  selector:
    app: bms
  ports:
  - port: 80
    targetPort: 3000
    nodePort: 30007
